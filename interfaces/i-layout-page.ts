import { ReactNode } from 'react';

export type LayoutProps = {
  title: string;
  description: string;
  // children: ReactNode;
  ogImageUrl: string;
  ogUrl: string;
  ogTitle: string;
  ogDescription: string;
};
